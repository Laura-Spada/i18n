PORT=4000
# URL do endpoint SOAP (no nosso servidor, é o mesmo caminho usado para POST do envelope)
SOAP_WSDL_URL=https://localhost:8443/wsdl

# Em DEV com certificado autoassinado: true (NÃO usar em produção)
ALLOW_INSECURE_TLS=true

# Credenciais default para testes (opcional; o cliente pode enviar outras)
AUTH_USER=admin
AUTH_PASS=secret
